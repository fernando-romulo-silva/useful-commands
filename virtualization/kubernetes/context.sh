# Check the current context:

$ kubectl config current-context

# Get all context

$ kubectl config get-contexts

# Change the context:

$ kubectl config use-context fernando-silva-myconpany-cluster-name01

$ kubectl config use-context fernando-silva-myconpany-cluster-name02
